---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex gap-2">
  {
    tags.map((tag) => (
      <span class="flex items-center text-[11px] text-muted/60 border border-muted/30 px-1.5 h-5 rounded">
        {tag}
      </span>
    ))
  }
</div>
