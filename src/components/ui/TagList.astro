---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex gap-2">
  {
    tags.map((tag) => (
      <span class="flex items-center text-[12px] text-muted/60 border border-muted/30 px-1.5 h-6 rounded">
        {tag}
      </span>
    ))
  }
</div>
