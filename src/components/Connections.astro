---
const users = [
  "thatmagicalcat",
  "duskyelf",
  "birbuh",
  "PranavU-Coder",
  "ani-2008",
  "infiknighte",
];

const connections = users.map((username) => ({
  username,
  avatar: `/avatars/${username}.png`,
  url: `https://github.com/${username}`,
}));
---

<section class="mx-4 md:mx-16 mb-20">
  <h1 class="text-foreground/95 text-lg font-semibold">
    # cool friends <span class="text-muted text-sm"
      >- do checkout their stuff</span
    >
  </h1>

  <div
    class="grid grid-cols-[repeat(6,56px)] md:grid-cols-[repeat(6,58px)] auto-rows-[56px] gap-2 mt-4"
  >
    {
      connections.map((friend) => (
        <a
          href={friend?.url}
          target="_blank"
          class="h-16 w-16 relative overflow-hidden border border-border shadow-sm shadow-white/10 hover:scale-105 transition-transform duration-300 ease-out"
        >
          <div class="absolute inset-0 bg-foreground/10 blur-3xl scale-125" />

          <img
            loading="lazy"
            decoding="async"
            fetchpriority="high"
            src={friend?.avatar}
            alt={friend?.username}
            class="h-full w-full object-cover text-muted wrap-break-word hover:scale-110 transition-transform duration-300 ease-out will-change-transform"
          />
        </a>
      ))
    }
  </div>
</section>
